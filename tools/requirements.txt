# Requirements for website-auditor knowledge_updater tool
# Install: pip install -r tools/requirements.txt

# crawl4ai — primary web crawling engine
crawl4ai>=0.3.0

# requests — fallback HTTP client
requests>=2.28.0

# beautifulsoup4 — HTML parsing for fallback mode
beautifulsoup4>=4.12.0

# python-dateutil — flexible date parsing
python-dateutil>=2.8.0